# IDE / editors
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Secrets / local config — never commit
.env
.env.local
.env.*.local
*.pem
*.key

# Локальный bootstrap-манифест конкретной установки (см. manifest.example.yaml).
# Содержит список проектов/идентичностей для конкретного инстанса и в git не едет.
mcp-kanban/bootstrap/manifest.yaml

# SLONK-6: кастомные agent-роли в `roles/`. Дефолтные 7 ролей и README
# коммитятся, любые другие `*.md` — нет (пользователь добавляет свои
# роли локально, они описывают конкретную инсталляцию).
mcp-kanban/roles/*.md
!mcp-kanban/roles/README.md
!mcp-kanban/roles/analyst-agent.md
!mcp-kanban/roles/developer-agent.md
!mcp-kanban/roles/security-auditor-agent.md
!mcp-kanban/roles/code-review-agent.md
!mcp-kanban/roles/qa-agent.md
!mcp-kanban/roles/doc-agent.md
!mcp-kanban/roles/merger-agent.md

# Node / pnpm (MCP server, Phase 2+)
node_modules/
dist/
build/
coverage/
.nyc_output/
*.tsbuildinfo
.pnpm-store/
.pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker / runtime state — volumes никогда не идут в git
mcp_data/
mcp_logs/
postgres_data/
redis_data/
rabbitmq_data/
minio_data/
plane_uploads/
caddy_data/
caddy_config/
prometheus_data/
grafana_data/
loki_data/
backup_data/

# Бэкапы / дампы
*.dump
*.sql.gz
*.tgz
*.tar.gz
/backup/local/

# SQLite (локальные стораджи MCP)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Логи
*.log
logs/

# Сгенерированные JSON Schema (из zod) — генерируются в build-step
mcp-kanban/schemas/*.json

# Claude Code session artifacts (loop/cron locks, local settings)
.claude/
